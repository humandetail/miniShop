<!--pages/profile/profile.wxml-->
<view class="profile-page">
  <pro-header />
  <pro-navigator navigator="{{ navigator }}" />

  <pro-section cartNumber="{{ cartNumber }}" />

  <pro-order order="{{ order }}" />
</view>
